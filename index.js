const createPropCases = Component => {
  const { propTypes } = Component
  const propNames = Object.keys(propTypes)

  for (var i = 0; i < propNames.length; i++) {
    propNames[i]
  }
}

module.exports = { createPropCases }
